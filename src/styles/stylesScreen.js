
import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
        padding: 10,
    },
    text: {
        textAlign: 'center',
        marginTop: 24,
        fontSize: 25,
        fontWeight: "bold",
        marginBottom: 24
    },
    input: {
        marginBottom: 10,
    },
    button:{
        marginBottom:10,
        padding:5,
        borderRadius:5

    }
});

export default styles;